this project works with old table API or cosmosdb table api just by changing connection string. 

to work better use new sdk instead of old. in the other project.

note: old normal table api sdk also works with new cosmosdb table api by communicating using api gateway. but better to use new